import React from 'react';
import MenuPanel from './MenuPanel.component';

export default {
  title: "Components/MenuPanel",
  component: MenuPanel,
}

const Template = (args) => <MenuPanel {...args} />;
export const Default = Template.bind({});

